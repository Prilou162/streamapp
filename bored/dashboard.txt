<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard – STREAMLATAM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- ICONS -->
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
  />

  <style>
    :root {
      --bg: #05060a;
      --bg-elevated: #080816;
      --bg-card: #101222;
      --bg-soft: #181a2f;
      --border-soft: #1f213a;
      --border-subtle: #141626;
      --text-main: #f4f4fb;
      --text-soft: #9c9fc2;
      --text-muted: #7a7ea5;
      --accent: #5b4bff;
      --accent-soft: #7b6bff;
      --success: #5bff94;
      --danger: #ff5b7d;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: system-ui, Inter, sans-serif;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background:
        radial-gradient(circle at top left, rgba(91,75,255,0.2), transparent 55%),
        radial-gradient(circle at bottom right, rgba(91,255,148,0.12), transparent 55%),
        linear-gradient(140deg, #05060a, #05050d 40%, #05060a);
      color: var(--text-main);
    }

    .app-shell {
      display: flex;
      min-height: 100vh;
    }

    /* SIDEBAR (alineada con campaigns) */
    .sidebar {
      width: 260px;
      background:
        radial-gradient(circle at top, rgba(91,75,255,0.25), transparent 60%),
        #05050b;
      border-right: 1px solid var(--border-subtle);
      padding: 22px 18px;
      display: flex;
      flex-direction: column;
      gap: 30px;
      position: sticky;
      top: 0;
      height: 100vh;
    }

    .sidebar-logo {
      font-size: 19px;
      font-weight: 600;
      letter-spacing: .12em;
    }
    .sidebar-logo span { color: var(--accent-soft); }

    .sidebar-section-title {
      font-size: 11px;
      letter-spacing: .1em;
      text-transform: uppercase;
      color: #63678a;
      margin-bottom: 6px;
    }

    .sidebar-nav { display: flex; flex-direction: column; gap: 4px; }

    .nav-item {
      padding: 9px 12px;
      border-radius: 12px;
      font-size: 14px;
      display: flex;
      align-items: center;
      gap: 10px;
      color: var(--text-soft);
      cursor: pointer;
      transition: .15s;
      position: relative;
    }

    .nav-item a {
      color: inherit;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 10px;
      width: 100%;
    }

    .nav-item:hover {
      background: #181a2e;
      color: #fff;
      transform: translateY(-1px);
    }

    .nav-item.active {
      background:
        radial-gradient(circle at top left, rgba(91,75,255,.38), transparent 60%),
        #16182d;
      box-shadow: 0 12px 32px rgba(0,0,0,.55);
      color: #fff;
    }

    /* TOPBAR */
    .topbar {
      padding: 14px 22px;
      border-bottom: 1px solid var(--border-subtle);
      background:
        radial-gradient(circle at top left, rgba(91,75,255,0.3), transparent 60%),
        rgba(5,5,12,0.92);
      backdrop-filter: blur(12px);
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky; top: 0; z-index: 10;
    }

    .topbar-title { font-size: 17px; font-weight: 600; }
    .topbar-sub { font-size: 12px; color: var(--text-soft); }

    .topbar-right {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 11px;
    }

    .pill-small {
      padding: 3px 8px;
      border-radius: 999px;
      border: 1px solid var(--border-soft);
      background: #141529;
      font-size: 11px;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }

    /* CONTENT */
    .content {
      display: flex;
      flex-direction: column;
      gap: 18px;
      padding: 22px;
    }

    .row {
      display: grid;
      grid-template-columns: 2fr 1.4fr;
      gap: 18px;
    }

    .row-full {
      display: grid;
      grid-template-columns: 1.4fr 1.6fr;
      gap: 18px;
    }

    /* GLOBAL BUTTON HOVERS (por si usamos botones acá o en otras páginas) */
    button,
    .btn-primary,
    .btn-ghost,
    .save-btn,
    .edit-toggle-btn {
      transition:
        background 0.18s ease,
        box-shadow 0.18s ease,
        transform 0.18s ease,
        border-color 0.18s ease;
    }

    button:hover,
    .btn-primary:hover,
    .save-btn:hover,
    .edit-toggle-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 12px 30px rgba(0,0,0,0.75);
    }

    /* CARDS */
    .card {
      background:
        linear-gradient(130deg, rgba(255,255,255,0.02), transparent 40%),
        var(--bg-card);
      border: 1px solid rgba(255,255,255,0.05);
      border-radius: 20px;
      padding: 18px 20px;
      box-shadow: 0 8px 24px rgba(0,0,0,.45);
      transition: 0.2s ease;
      animation: fadeInUp 0.2s ease-out;
    }

    .card:hover {
      box-shadow: 0 12px 34px rgba(0,0,0,.55);
    }

    .card-title { font-size: 15px; font-weight: 600; margin-bottom: 4px; }
    .card-sub { font-size: 12px; color: var(--text-soft); margin-bottom: 10px; }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(4px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* SNAPSHOT */
    .snapshot-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 10px;
    }

    .avatar-circle-lg {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      background:
        conic-gradient(from 210deg, #5b4bff, #5bff94, #ffc857, #ff5b7d, #5b4bff);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 18px;
      color: #fff;
      box-shadow: 0 0 18px rgba(0,0,0,0.6);
      flex-shrink: 0;
    }

    .snapshot-name {
      font-size: 16px;
      font-weight: 600;
    }

    .snapshot-tagline {
      font-size: 12px;
      color: var(--text-soft);
    }

    .snapshot-grid {
      display: grid;
      grid-template-columns: repeat(3,minmax(0,1fr));
      gap: 10px;
      margin-top: 10px;
    }

    .mini-stat {
      background: #15172b;
      border-radius: 14px;
      padding: 9px 10px;
      border: 1px solid var(--border-subtle);
      font-size: 12px;
    }

    .mini-label {
      font-size: 11px;
      color: var(--text-soft);
      margin-bottom: 2px;
    }

    .mini-value {
      font-size: 14px;
      font-weight: 600;
    }

    /* NIVEL ATRACTIVO / INSIGHTS */
    .score-pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 9px;
      border-radius: 999px;
      font-size: 11px;
      border: 1px solid rgba(91,255,148,0.8);
      color: var(--success);
      background: rgba(91,255,148,0.08);
      margin-bottom: 8px;
    }

    .score-pill.mid {
      border-color: #ffc857;
      color: #ffc857;
      background: rgba(255,200,87,0.08);
    }

    .score-pill.low {
      border-color: #7a7ea5;
      color: #9c9fc2;
      background: rgba(122,126,165,0.08);
    }

    .score-bar-wrapper {
      margin-top: 6px;
      margin-bottom: 8px;
    }

    .score-bar-bg {
      width: 100%;
      height: 7px;
      border-radius: 999px;
      background: #141529;
      overflow: hidden;
    }

    .score-bar-fill {
      height: 100%;
      width: 0;
      border-radius: 999px;
      background: linear-gradient(90deg, #ffc857, #5bff94, #5b4bff);
      transition: width 0.35s ease-out;
    }

    .score-bar-label {
      font-size: 11px;
      color: var(--text-soft);
      margin-top: 4px;
    }

    .insights-list {
      font-size: 12px;
      color: var(--text-soft);
      padding-left: 16px;
      margin-top: 4px;
    }

    .insights-list li {
      margin-bottom: 4px;
    }

    /* BLOQUE “QUÉ PODRÍAS HACER ESTE MES” */
    .month-actions {
      display: flex;
      flex-direction: column;
      gap: 8px;
      font-size: 12px;
    }

    .month-item {
      background: #15172b;
      border-radius: 14px;
      padding: 8px 10px;
      border: 1px solid var(--border-subtle);
      display: flex;
      gap: 8px;
    }

    .month-icon {
      width: 22px;
      height: 22px;
      border-radius: 999px;
      background: #141529;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
    }

    .month-main {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .month-title {
      font-size: 12px;
      font-weight: 500;
    }

    .month-desc {
      font-size: 11px;
      color: var(--text-soft);
    }

    /* RESUMEN POSTULACIONES */
    .summary-grid {
      display: grid;
      grid-template-columns: repeat(3,minmax(0,1fr));
      gap: 10px;
      margin-top: 8px;
    }

    .summary-box {
      background: #15172b;
      border-radius: 14px;
      padding: 9px 10px;
      border: 1px solid var(--border-subtle);
      font-size: 12px;
    }

    .summary-label {
      font-size: 11px;
      color: var(--text-soft);
    }

    .summary-value {
      font-size: 15px;
      font-weight: 600;
    }

    .summary-sub {
      font-size: 11px;
      color: var(--text-soft);
    }

    /* PROGRESO DE OBJETIVOS */
    .goal-section {
      margin-top: 14px;
      border-top: 1px dashed rgba(255,255,255,0.07);
      padding-top: 10px;
    }

    .goal-section-title {
      font-size: 12px;
      font-weight: 500;
      margin-bottom: 6px;
    }

    .goal-row {
      margin-bottom: 8px;
    }

    .goal-label-line {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      font-size: 11px;
      color: var(--text-soft);
      margin-bottom: 3px;
    }

    .goal-main-label {
      font-weight: 500;
      color: #f4f4fb;
    }

    .goal-bar-bg {
      width: 100%;
      height: 6px;
      border-radius: 999px;
      background: #141529;
      overflow: hidden;
    }

    .goal-bar-fill {
      height: 100%;
      width: 0;
      border-radius: 999px;
      background: linear-gradient(90deg, #5b4bff, #7b6bff);
      transition: width 0.35s ease-out;
    }

    .goal-meta {
      font-size: 11px;
      color: var(--text-soft);
      margin-top: 2px;
    }

    @media (max-width: 980px) {
      .sidebar { display:none; }
      .content { padding: 16px; }
      .row, .row-full {
        grid-template-columns: 1fr;
      }
      .snapshot-grid,
      .summary-grid {
        grid-template-columns: repeat(2,minmax(0,1fr));
      }
    }

    @media (max-width: 720px) {
      .snapshot-grid,
      .summary-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>
<div class="app-shell">

  <!-- SIDEBAR -->
  <aside class="sidebar">
    <div class="sidebar-logo">STREAM<span>LATAM</span></div>

    <div>
      <div class="sidebar-section-title">Navegación</div>
      <nav class="sidebar-nav">
        <div class="nav-item active"><a href="dashboard.html">🏠 Dashboard</a></div>
        <div class="nav-item"><a href="campaigns.html">🎯 Campañas</a></div>
        <div class="nav-item"><a href="applications.html">📩 Mis postulaciones</a></div>
      </nav>
    </div>

    <div>
      <div class="sidebar-section-title">Cuenta</div>
      <nav class="sidebar-nav">
        <div class="nav-item"><a href="profile.html">🧑‍💻 Perfil</a></div>
        <div class="nav-item">⚙️ Configuración</div>
      </nav>
    </div>
  </aside>

  <!-- MAIN -->
  <main class="main-area">

    <!-- TOPBAR -->
    <header class="topbar">
      <div>
        <div class="topbar-title">Tu panel como streamer</div>
        <div class="topbar-sub">Resumen rápido + insights pensados para sponsors.</div>
      </div>
      <div class="topbar-right">
        <span class="pill-small">Versión demo</span>
        <span class="topbar-sub" id="topbarSummary">
          Datos cargados localmente (perfil demo)
        </span>
      </div>
    </header>

    <!-- CONTENT -->
    <section class="content">

      <!-- FILA 1: Snapshot + Insights -->
      <div class="row">
        <!-- SNAPSHOT -->
        <div class="card">
          <div class="card-title">Snapshot de tu canal</div>
          <div class="card-sub">Estos datos salen de tu perfil y se usan para recomendarte campañas.</div>

          <div class="snapshot-header">
            <div class="avatar-circle-lg" id="snapAvatar">F</div>
            <div>
              <div class="snapshot-name" id="snapName">FrancoDota</div>
              <div class="snapshot-tagline" id="snapRegion">
                LATAM (AR) · Español
              </div>
            </div>
          </div>

          <div class="snapshot-grid">
            <div class="mini-stat">
              <div class="mini-label">Viewers concurrentes aprox.</div>
              <div class="mini-value" id="snapViewers">145</div>
            </div>
            <div class="mini-stat">
              <div class="mini-label">Categorías principales</div>
              <div class="mini-value" id="snapCategories">Dota 2 · Just Chatting</div>
            </div>
            <div class="mini-stat">
              <div class="mini-label">Zona horaria principal</div>
              <div class="mini-value" id="snapTimezone">ART</div>
            </div>
          </div>

          <div style="margin-top:10px;font-size:11px;color:var(--text-soft);">
            Si actualizás los datos base en tu perfil, este snapshot se ajusta automáticamente (demo).
          </div>
        </div>

        <!-- INSIGHTS PARA SPONSORS -->
        <div class="card">
          <div class="card-title">Insights de tu canal para sponsors (demo)</div>
          <div class="card-sub">
            Pequeños resúmenes que ayudan a una marca a entender dónde estás parado.
          </div>

          <div id="attractivenessPill" class="score-pill">
            <i class="fa-solid fa-signal"></i>
            <span id="attractivenessLabel">Listo para primeras campañas pequeñas</span>
          </div>

          <div class="score-bar-wrapper">
            <div class="score-bar-bg">
              <div id="scoreFill" class="score-bar-fill"></div>
            </div>
            <div class="score-bar-label" id="scoreText">
              Estás en un rango donde ya tiene sentido probar campañas simples.
            </div>
          </div>

          <ul class="insights-list" id="insightsList">
            <li>Tu rango de viewers indica que podrías empezar con menciones cortas y paneles simples.</li>
            <li>Las categorías que streameás son atractivas para marcas de hardware y fintech.</li>
          </ul>
        </div>
      </div>

      <!-- FILA 2: Qué podrías hacer este mes + Resumen de postulaciones -->
      <div class="row-full">
        <!-- ACCIONES DEL MES -->
        <div class="card">
          <div class="card-title">Qué podrías hacer este mes (demo)</div>
          <div class="card-sub">
            Ideas pensadas según tu rango de viewers y la etapa en la que estás.
          </div>

          <div class="month-actions" id="monthActions">
            <!-- Se completa por JS según rango -->
          </div>
        </div>

        <!-- RESUMEN POSTULACIONES + OBJETIVOS -->
        <div class="card">
          <div class="card-title">Resumen de tus postulaciones (demo)</div>
          <div class="card-sub">
            En una versión real, esto se llenaría con tus campañas aplicadas.
          </div>

          <div class="summary-grid">
            <div class="summary-box">
              <div class="summary-label">Postulaciones enviadas</div>
              <div class="summary-value" id="sumApplied">3</div>
              <div class="summary-sub">Últimos 30 días</div>
            </div>
            <div class="summary-box">
              <div class="summary-label">En revisión</div>
              <div class="summary-value" id="sumReview">1</div>
              <div class="summary-sub">Esperando respuesta</div>
            </div>
            <div class="summary-box">
              <div class="summary-label">Aprobadas</div>
              <div class="summary-value" id="sumApproved">2</div>
              <div class="summary-sub">Incluye campañas demo</div>
            </div>
          </div>

          <!-- NUEVO: Progreso hacia objetivos -->
          <div class="goal-section">
            <div class="goal-section-title">
              Progreso hacia tus objetivos del mes (demo)
            </div>

            <div class="goal-row">
              <div class="goal-label-line">
                <span class="goal-main-label">Postulaciones este mes</span>
                <span id="goalAppliedLabel">0 / 5</span>
              </div>
              <div class="goal-bar-bg">
                <div id="goalAppliedBar" class="goal-bar-fill"></div>
              </div>
              <div class="goal-meta">
                Meta sugerida: enviar al menos 5 postulaciones a campañas alineadas con tu canal.
              </div>
            </div>

            <div class="goal-row">
              <div class="goal-label-line">
                <span class="goal-main-label">Campañas aprobadas</span>
                <span id="goalApprovedLabel">0 / 3</span>
              </div>
              <div class="goal-bar-bg">
                <div id="goalApprovedBar" class="goal-bar-fill"></div>
              </div>
              <div class="goal-meta">
                Meta sugerida: asegurar 2–3 campañas aprobadas para tener casos de éxito que mostrar.
              </div>
            </div>
          </div>

          <div style="margin-top:10px;font-size:11px;color:var(--text-soft);">
            Este bloque te sirve en la demo para explicar que la plataforma no sólo lista campañas,
            también te ayuda a pensar cuántas estás moviendo por mes.
          </div>
        </div>
      </div>

    </section>
  </main>
</div>

<script>
  // Leer datos base del localStorage
  function getBaseData() {
    const defaultData = {
      name: "FrancoDota",
      viewers: "145",
      categories: "Dota 2 · Just Chatting",
      region: "LATAM (AR) · Español",
      timezone: "ART"
    };

    try {
      const stored = JSON.parse(localStorage.getItem("streamerBaseData"));
      if (stored && typeof stored === "object") {
        return {
          name: stored.name || defaultData.name,
          viewers: stored.viewers || defaultData.viewers,
          categories: stored.categories || defaultData.categories,
          region: stored.region || defaultData.region,
          timezone: stored.timezone || defaultData.timezone
        };
      }
    } catch (e) {
      // ignore
    }
    return defaultData;
  }

  const base = getBaseData();

  // --- Snapshot ---
  const snapAvatar = document.getElementById("snapAvatar");
  const snapName = document.getElementById("snapName");
  const snapRegion = document.getElementById("snapRegion");
  const snapViewers = document.getElementById("snapViewers");
  const snapCategories = document.getElementById("snapCategories");
  const snapTimezone = document.getElementById("snapTimezone");
  const topbarSummary = document.getElementById("topbarSummary");

  if (snapName) snapName.textContent = base.name;
  if (snapAvatar) snapAvatar.textContent = (base.name || "S").trim().charAt(0).toUpperCase();
  if (snapRegion) snapRegion.textContent = base.region;
  if (snapViewers) snapViewers.textContent = base.viewers;
  if (snapCategories) snapCategories.textContent = base.categories;
  if (snapTimezone) snapTimezone.textContent = base.timezone;
  if (topbarSummary) {
    topbarSummary.textContent = `Datos base cargados: ${base.viewers} viewers aprox. · ${base.timezone}`;
  }

  // Parse viewers a número
  function parseViewers(v) {
    if (!v) return 0;
    const cleaned = String(v).replace(/[^\d]/g, "");
    const n = parseInt(cleaned, 10);
    return isNaN(n) ? 0 : n;
  }

  const viewersNum = parseViewers(base.viewers);

  // --- Nivel de atractivo para marcas ---
  const attractivenessPill = document.getElementById("attractivenessPill");
  const attractivenessLabel = document.getElementById("attractivenessLabel");
  const scoreFill = document.getElementById("scoreFill");
  const scoreText = document.getElementById("scoreText");
  const insightsList = document.getElementById("insightsList");

  let score = 20; // 0–100
  let level = "low";
  let label = "En etapa temprana, ideal para construir comunidad";
  let text = "Todavía es pronto para campañas grandes. Enfocate en retención de viewers y engagement.";
  let extraInsights = [];

  if (viewersNum < 50) {
    score = 25;
    level = "low";
    label = "Etapa temprana · foco en comunidad";
    text = "Todavía es pronto para sponsors grandes, pero sos atractivo para marcas pequeñas/locales.";
    extraInsights.push("Probá códigos de descuento simples o giveaways con marcas chicas.");
    extraInsights.push("Mostrá en tu perfil cómo crece tu comunidad mes a mes.");
  } else if (viewersNum < 120) {
    score = 55;
    level = "mid";
    label = "Listo para primeras campañas pequeñas";
    text = "Tu rango de viewers ya permite campañas de prueba con fees bajos o producto + fee.";
    extraInsights.push("Las marcas fintech, hardware y snacks suelen probar con este rango.");
    extraInsights.push("Acordá campañas cortas (1–2 semanas) para mostrar resultados rápido.");
  } else if (viewersNum < 250) {
    score = 75;
    level = "mid";
    label = "Rango fuerte para sponsors nicho";
    text = "Ya sos interesante para marcas que buscan comunidades específicas y cercanas.";
    extraInsights.push("Podés negociar fee fijo + variable (por clics o registros).");
    extraInsights.push("Mostrá clip de mejores momentos y estabilidad de tus viewers.");
  } else {
    score = 90;
    level = "high";
    label = "Muy atractivo para sponsors serios";
    text = "Tu tamaño permite campañas más estructuradas y contratos mensuales.";
    extraInsights.push("Podés pedir mayor fee fijo y exigir mejores condiciones.");
    extraInsights.push("Mostrá datos de retención y engagement (chat activo, repetición de viewers).");
  }

  if (attractivenessPill) {
    attractivenessPill.classList.remove("low", "mid");
    if (level === "low") attractivenessPill.classList.add("low");
    if (level === "mid") attractivenessPill.classList.add("mid");
    // nivel alto = estilo default (verde)
  }
  if (attractivenessLabel) attractivenessLabel.textContent = label;
  if (scoreFill) {
    requestAnimationFrame(() => {
      scoreFill.style.width = score + "%";
    });
  }
  if (scoreText) scoreText.textContent = text;

  if (insightsList) {
    insightsList.innerHTML = "";
    extraInsights.forEach(msg => {
      const li = document.createElement("li");
      li.textContent = msg;
      insightsList.appendChild(li);
    });
    if (extraInsights.length === 0) {
      const li = document.createElement("li");
      li.textContent = "Usá este panel para explicar a las marcas cómo se ve tu canal hoy.";
      insightsList.appendChild(li);
    }
  }

  // --- Qué podrías hacer este mes (según viewers) ---
  const monthActions = document.getElementById("monthActions");
  if (monthActions) {
    const items = [];

    if (viewersNum < 50) {
      items.push({
        icon: "fa-people-group",
        title: "Fortalecer tu núcleo de comunidad",
        desc: "Hacé streams más largos 1–2 veces por semana, con foco en hablar con el chat. Eso hace que futuros sponsors vean comunidad real, no solo números."
      });
      items.push({
        icon: "fa-gift",
        title: "Probar sorteos con marcas pequeñas",
        desc: "Contactá comercios locales o tiendas chicas online para hacer un giveaway simple a cambio de visibilidad. Es un primer paso sólido."
      });
    } else if (viewersNum < 120) {
      items.push({
        icon: "fa-bullhorn",
        title: "Armar tu “kit” para sponsors",
        desc: "Prepará un texto corto de presentación, tus métricas principales y 2–3 ejemplos de cómo mencionarías una marca en stream."
      });
      items.push({
        icon: "fa-diagram-project",
        title: "Testear 1 campaña demo",
        desc: "Elegí una marca ficticia o amiga y simulá una campaña con overlay, panel y mención. Eso sirve de caso de uso real para mostrar."
      });
    } else if (viewersNum < 250) {
      items.push({
        icon: "fa-chart-line",
        title: "Mostrar estabilidad de viewers",
        desc: "Clipeá o registrá varios streams donde mantengas buen pico de viewers y úsalo como argumento al negociar campañas."
      });
      items.push({
        icon: "fa-handshake",
        title: "Negociar fee + bonus",
        desc: "Proponé un fee fijo por campaña y un bonus por objetivos (clics, registros, uso de código). Eso te posiciona como alguien profesional."
      });
    } else {
      items.push({
        icon: "fa-briefcase",
        title: "Estructurar tu media kit completo",
        desc: "Armar un PDF simple con métricas, ejemplos de campañas, redes y horarios. En este rango ya hace diferencia."
      });
      items.push({
        icon: "fa-users-viewfinder",
        title: "Seleccionar marcas que encajen bien",
        desc: "Ya podés permitirte rechazar sponsors que no encajan. Eso mejora la percepción de tu canal y el long-term value."
      });
    }

    // Común para todos los rangos
    items.push({
      icon: "fa-pen",
      title: "Mantener tu perfil actualizado",
      desc: "Cada vez que cambie tu promedio de viewers, horarios o categorías principales, actualizalo en tu perfil para que los insights sigan siendo útiles."
    });

    items.forEach(item => {
      const wrapper = document.createElement("div");
      wrapper.className = "month-item";

      const iconDiv = document.createElement("div");
      iconDiv.className = "month-icon";
      iconDiv.innerHTML = `<i class="fa-solid ${item.icon}"></i>`;

      const main = document.createElement("div");
      main.className = "month-main";

      const title = document.createElement("div");
      title.className = "month-title";
      title.textContent = item.title;

      const desc = document.createElement("div");
      desc.className = "month-desc";
      desc.textContent = item.desc;

      main.appendChild(title);
      main.appendChild(desc);

      wrapper.appendChild(iconDiv);
      wrapper.appendChild(main);

      monthActions.appendChild(wrapper);
    });
  }

  // --- Progreso hacia objetivos del mes (nuevo) ---
  const sumAppliedEl = document.getElementById("sumApplied");
  const sumApprovedEl = document.getElementById("sumApproved");

  const appliedCurrent = sumAppliedEl ? parseInt(sumAppliedEl.textContent, 10) || 0 : 0;
  const approvedCurrent = sumApprovedEl ? parseInt(sumApprovedEl.textContent, 10) || 0 : 0;

  const appliedGoal = 5;
  const approvedGoal = 3;

  const goalAppliedLabel = document.getElementById("goalAppliedLabel");
  const goalApprovedLabel = document.getElementById("goalApprovedLabel");
  const goalAppliedBar = document.getElementById("goalAppliedBar");
  const goalApprovedBar = document.getElementById("goalApprovedBar");

  if (goalAppliedLabel) {
    goalAppliedLabel.textContent = `${appliedCurrent} / ${appliedGoal}`;
  }
  if (goalApprovedLabel) {
    goalApprovedLabel.textContent = `${approvedCurrent} / ${approvedGoal}`;
  }

  function calcPercent(current, goal) {
    if (!goal || goal <= 0) return 0;
    const pct = (current / goal) * 100;
    return Math.max(0, Math.min(100, pct));
  }

  const appliedPct = calcPercent(appliedCurrent, appliedGoal);
  const approvedPct = calcPercent(approvedCurrent, approvedGoal);

  if (goalAppliedBar) {
    requestAnimationFrame(() => {
      goalAppliedBar.style.width = appliedPct + "%";
    });
  }
  if (goalApprovedBar) {
    requestAnimationFrame(() => {
      goalApprovedBar.style.width = approvedPct + "%";
    });
  }
</script>

</body>
</html>
